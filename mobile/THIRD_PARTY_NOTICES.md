# Animation attribution

The bottom-aligned Size Animation 1 used for page navigation is adapted from
**Flutter Animation Gallery**, from the code example supplied for this project.
Credit: Flutter Animation Gallery.

The example permits project reuse with attribution. Its original notice reads:

> Feel free to use the code in your projects but do not forget to give me the credits adding my app (Flutter Animation Gallery) where you are gonna use it.

Implementation: `lib/core/theme/motion.dart` and `lib/core/routing/animated_route.dart`.
